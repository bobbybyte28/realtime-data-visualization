# realtime-data-visualization
The system will collect live data (binance)), store it in a distributed database, and convert it into live charts using a frontend UI. It will also provide an API so others can access this processed data. 
